# 📚 Trae 商业化字典技能库 (CloudShu Dictionary Skills v7.0)

## 🌟 核心理念 (Core Philosophy)

**“单字即能力，成语即流程。”**

本项目是一个专为 Trae IDE 设计的**原生技能生态系统**。它摒弃了复杂的中间件引擎，回归到最纯粹的“文件系统即路由”哲学，利用五行分类体系将软件工程中的原子能力进行结构化管理。

- **去中心化 (Decentralized)**: 没有单一的 `engine.py`，每个技能都是独立的 Python 包。
- **字典优先 (Dictionary First)**: 所有的能力调用都始于对“字典”的查阅。
- **原生兼容 (Native Compatible)**: 完全遵循 Claude/MCP Skill 标准架构。

---

## 🏗️ 架构概览 (Architecture)

技能库采用 **[五行分类] -> [技能目录] -> [执行脚本]** 的三层架构：

```text
.trae/skills/dictionary/
├── SKILL.md                  # [L1] 总路由表 (The Dictionary)
├── water_knowledge/          # [L2] 五行分类：[水] 知识系
│   ├── INDEX.md              #      分类索引
│   ├── 搜/                   # [L3] 原子技能：[搜]
│   │   ├── SKILL.md          #      技能元数据 (Frontmatter)
│   │   └── scripts/
│   │       └── main.py       #      执行入口
│   └── 读/                   # [L3] 原子技能：[读]
├── wood_creation/            #      五行分类：[木] 创造系
│   └── 写/                   #      原子技能：[写]
...
```

---

## 🛠️ 使用指南 (User Guide)

### 1. 基础调用流程

当 Trae 接收到用户指令（如“帮我查一下 Rust 的文档”）时，它会遵循以下 **P0 协议**：

1.  **查字典**: 读取 `.trae/skills/dictionary/SKILL.md`，确认“查询文档”属于 **[水] 知识系**。
2.  **找技能**: 进入 `water_knowledge/INDEX.md`，找到对应技能 **[搜]**。
3.  **执行**: 调用原生脚本：
    ```bash
    python .trae/skills/dictionary/water_knowledge/搜/scripts/main.py --input '{"query": "Rust documentation"}'
    ```

### 2. 五行分类说明

| 系别 (Element) | 意图 (Intent) | 典型技能 (Skills) |
| :--- | :--- | :--- |
| **💧 [水] 知识系** | 获取信息、读取内容、连接外部 | **搜** (Search), **读** (Read) |
| **🌲 [木] 创造系** | 生成代码、写入文件、创建项目 | **写** (Write/Generate) |
| **🔥 [火] 分析系** | 诊断错误、分析性能、审查日志 | **判** (Analyze/Diagnose) |
| **🌏 [土] 基建系** | 管理依赖、配置环境、部署运维 | **管** (Manage/Deploy) |
| **⚔️ [金] 优化系** | 重构代码、安全加固、格式化 | **改** (Refactor/Optimize) |
| **📜 [成语] 组合** | 处理复杂的、多步骤的业务流程 | **日新月异** (Update Workflow) |

---

## ⚡ 扩展开发 (Development)

我们提供了一个强大的 **元技能 (Meta-Skill)** —— `agent-skill-creator`，用于自动生成标准化的新技能。

**生成新技能命令示例**:
```bash
python .trae/skills/agent-skill-creator/scripts/generate_skill.py \
  --name "观" \
  --category "fire_analysis" \
  --description "图像识别与视觉分析" \
  --output ".trae/skills/dictionary"
```

该命令会自动创建目录、`SKILL.md` (含 Frontmatter)、`scripts/main.py` 模板，并自动更新对应的 `INDEX.md` 索引文件。

---

## 💡 最佳实践 (Best Practices)

1.  **不要手写脚本**: 始终使用 `agent-skill-creator` 来创建新技能，以保证架构的一致性。
2.  **单一职责**: 每个“单字”技能只做一件事。复杂的任务请创建“成语”技能（组合调用多个单字）。
3.  **依赖管理**: 每个技能目录下都有独立的 `requirements.txt`，互不干扰。
