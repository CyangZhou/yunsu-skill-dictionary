# 云舒字典 - 商业化总规划白皮书

> **核心哲学**: "卖钥匙，不卖锁。" (Sell the Key, Not the Code)

---

## 1. 系统架构 (System Architecture)

这是一套 **混合云架构 (Hybrid Cloud Architecture)**，旨在平衡用户体验（本地极速）与知识产权保护（云端验证）。

```mermaid
graph LR
    User[用户 / 开发者] -->|聊天 / 指令| IDE[Trae / VS Code]
    IDE -->|读取| SkillMD[SKILL.md (说明书)]
    IDE -->|执行| ClientScript[main.py (客户端)]
    
    subgraph "护城河 (The Moat)"
        ClientScript -->|API 请求 + Key| CloudAPI[云端大脑 (Vercel)]
        CloudAPI -->|验证| DB[(Key 数据库)]
        CloudAPI -->|执行| SecretCode[核心算法 / AI 模型]
    end
    
    SecretCode -->|结果| CloudAPI
    CloudAPI -->|JSON| ClientScript
    ClientScript -->|响应| IDE
    IDE -->|回答| User
```

### 核心组件 (Components)
1.  **容器 (The Vessel)**: `云舒字典插件` / `Trae 原生包`。
    *   **角色**: 商店门面与执行环境。
    *   **状态**: 开源 / 免费 (可以随便发给别人)。
2.  **连接器 (The Connector)**: `main.py` (本地脚本)。
    *   **角色**: 一个只负责转发请求的“傻瓜客户端”。里面没有业务逻辑。
    *   **状态**: 分发给用户 (不怕被分享，因为没有 Key 就没用)。
3.  **大脑 (The Brain)**: `云端后端` (Serverless)。
    *   **角色**: 验证 Key 是否有效，并运行真正值钱的高级算法（如选股、代码重构）。
    *   **状态**: 私有 (部署在你自己的 Vercel 账号下)。

---

## 2. 商业闭环 (The Business Loop)

### 第一阶段：引流 (Acquisition)
*   **策略**: 免费分发 **Starter Kit** (启动包)。
*   **诱饵**: 用户可以免费使用基础工具，如 `ni-hao` (问候) 和 `ri-xin-yue-yi` (GitHub 日报)。
*   **效果**: 用户安装插件，习惯了这套工作流，并在控制面板中看到了“带锁”的高级技能。

### 第二阶段：转化 (Conversion)
*   **触发**: 用户点击高级技能（如 `cai-yuan-guang-jin`）的“解锁”按钮。
*   **门槛**: 系统提示输入 API Key。
*   **行动**: 用户联系你（或点击你的支付链接）购买 Key。

### 第三阶段：变现 (Monetization)
*   **交易**: 用户支付 50 元 -> 你生成一个 Key (`vip-user-001`)。
*   **激活**: 你将 `vip-user-001` 添加到你的云端后台（或数据库）。
*   **交付**: 用户在本地填入 Key，瞬间解锁高级功能。

### 第四阶段：留存与防守 (Retention & Defense)
*   **更新**: 你只需要更新 **服务器端** 的代码，用户就能获得更强的算法，无需重新下载。
*   **反盗版**: 如果发现 `vip-user-001` 同时有 100 个连接（被盗版分享），你可以在后台直接封禁该 Key。所有盗版副本立刻失效。

---

## 3. 你的行动指南 (Operator Manual)

### 第一步：部署大脑 (Deploy the Brain)
1.  进入 `dist/cloud-shu-backend-starter` 文件夹。
2.  注册 [Vercel](https://vercel.com) (免费版足够用)。
3.  安装 Vercel CLI (`npm i -g vercel`) 并运行 `vercel deploy`。
4.  **结果**: 你会获得一个 API 地址 (例如 `https://cloud-shu-api.vercel.app`)。

### 第二步：配置客户端 (Configure the Client)
1.  打开 `dist/cloud-shu-dictionary-starter/idioms/cai-yuan-guang-jin/main.py`。
2.  将代码中的 `API_URL` 修改为你刚刚获得的 Vercel 地址。
3.  **行动**: 将整个 `dist/cloud-shu-dictionary-starter` 文件夹重新压缩为 Zip。

### 第三步：分发 (Distribute)
1.  将 Zip 文件发送给你的用户群 / 社区。
2.  告诉他们：“解压后双击 `setup.bat` 即可安装。”

### 第四步：管理密钥 (Manage Keys)
*   **添加新用户**:
    *   打开你的 Vercel 项目 -> Settings -> Environment Variables。
    *   或者直接修改 `api/index.py` (如果只是简单的文件存储)。
    *   添加 `"new-customer-key": {"tier": "gold", "credits": 100}`。
    *   重新部署 (只需几秒钟)。

---

## 4. 为什么这能成 (Why This Works)

1.  **低摩擦 (Low Friction)**: 用户不需要懂 Docker 或 Python 环境配置。`setup.bat` 一键搞定。
2.  **高兼容 (High Compatibility)**: 原生支持 **Trae**, **VS Code**, **Cursor**, 以及任何 MCP 兼容的 Agent。
3.  **绝对控制 (Absolute Control)**: 你可以随时随地用手机关停任何用户的权限。

**状态**: 准备发射。 🚀
